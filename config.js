/**
 * ENHYPEN, WHERE TO START? runtime configuration
 *
 * The site works immediately with local browser storage when these values are empty.
 * To share FAN PICKS and reaction counts across visitors, create a Supabase project,
 * run supabase-schema.sql, then paste the Project URL and anon/publishable key below.
 */
window.ENHYPEN_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
  sharedFanMode: false
};
